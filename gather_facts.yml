---
 - hosts: all
   tasks:
   - debug:
      msg: 
       - "The os disribution is: {{ansible_facts.distribution}}"
       - "The os name is: {{ansible_system}}"
       - "The os family is: {{ansible_os_family}}"
